name: consulta-cedula
autocreate: true
image:
  name: okteto.dev/consulta-cedula:dev
  context: .
  # target: dev
command: bash
forward:
  - 8080:8080
sync:
  - .:/usr/src/app
persistentVolume:
  enabled: false